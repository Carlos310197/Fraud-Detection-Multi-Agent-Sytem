# fraud-detection-backend

Backend service for the multi-agent fraud detection system.

This file exists to satisfy the `readme = "README.md"` metadata in `pyproject.toml` so the package can be built by Hatchling.
